version: "3.9" 
services:
  web2:
    build: .
    ports:
      - "8000:5000"
    volumes:
      - .:/code
      - logvolume02:/var/log
    links:
      - redis2
  redis2:
    image: redis2
volumes:
  logvolume02: {}
